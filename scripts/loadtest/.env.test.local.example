# Copy this file to `.env.test.local` (same folder) and fill in the ID of a
# FRESH, BLANK Google Sheet that you created and shared with the service account
# as Editor. NEVER put the production sheet id here — the harness will refuse.
TEST_SHEET_ID=
